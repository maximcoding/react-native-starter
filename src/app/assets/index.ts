import User from '@/app/assets/svgs/user.svg';

export const ImagePng = {
  // PngImage: require('./images/example.png'),
};

export enum IconName {
  User = 'User',
}
export const AppIcon = {
  [IconName.User]: User,
};
